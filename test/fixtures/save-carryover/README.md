# Synthetic Local profile

This fixture is authored test data. It is not copied from a real YgoMaster save
and does not claim to document the complete `Player.json` schema. Tests use it
only to prove opaque byte preservation, malformed-input rejection, backup, and
stable-current promotion behavior.
